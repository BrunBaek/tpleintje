# Netlify serveert vanaf de wortel → pathPrefix "/" (geen /tpleintje/-subpad).
# GitHub Pages blijft intussen op /tpleintje/ dankzij de config-default.
[build]
  command = "npx @11ty/eleventy --pathprefix=/"
  publish = "_site"

# Decap CMS gebruikt Git Gateway via Netlify Identity (login).
